# Build Log — 2026-04-15

## Session goals

- Confirm architecture direction (Path A pure Apps Script vs Path B hybrid)
- Clarify that this is a formapprovals.com clone, not a custom form builder
- Evaluate formapprovals.com feature set and identify gaps
- Lock in schema-affecting feature decisions
- Set up Git and Obsidian
- Write and validate schema migration

## What happened

1. Reviewed Phase 1 deliverables (`schema.sql`, `config.php`, `supabase.php`, `index.php`, `auth-callback.php`) — all present and working.

2. Clarified product positioning: Aurora Approvals is a clone of formapprovals.com, not a custom form builder. Triggers are Google Form submissions, not custom forms created in the PHP app.

3. Evaluated Path A (pure Apps Script) vs Path B (hybrid). Confirmed Path B. See [[../decisions/ADR-001-path-b-hybrid]].

4. Mapped formapprovals.com feature set against current schema, identified gaps, discussed additional features beyond the original.

5. Locked in feature decisions. See [[../decisions/ADR-002-feature-additions]]:
   - Add Recommend and Acknowledge stage types
   - Add Quorum approvals
   - Add Dynamic recipients
   - Add Escalation rules (new table)
   - Add Delegation (new table)
   - Deferred features listed in ADR-002

6. Initialised Git repo and set up Obsidian vault structure under `docs/`.

7. Identified and documented two issues in Phase 1 files:
   - `config.php` contains Supabase service role key — added to `.gitignore`, created `config.example.php` template
   - `APP_URL` in `config.php` has a trailing period typo (line 16) — needs fixing

8. Wrote schema migration `migrations/2026-04-15_phase2_features.sql`. Non-destructive: two new tables, two new columns, two extended CHECK constraints.

9. Validated migration by running `schema.sql` + migration against an embedded Postgres in the sandbox. 16/16 constraint behaviour tests passed — every new validation accepts valid data and rejects invalid data as expected.

## What's next

- Run the migration in Supabase SQL Editor
- Fix the `APP_URL` typo in config.php (and rotate the exposed service role key as a precaution — it appeared in git setup chat)
- Begin Phase 2 build following the order in [[../phases/phase-2-plan]]

## Notes to self

- Don't edit the original `schema.sql` — treat it as the frozen Phase 1 baseline. All changes go via new files in `migrations/`.
- Apps Script work is Phase 4, not Phase 2. Phase 2 is admin UI only.
- The PHP webhook for form submissions must be idempotent on `(google_form_id, submitted_at)` to handle Apps Script retry semantics.
